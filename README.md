Minimal recreation of issue in mqtt gem

to show issue it has a couple of steps:
edit lib/test.rb to add credentials to access (had to remove for this upload)
```
bundle install
bundle exec ruby lib/test.rb
```
